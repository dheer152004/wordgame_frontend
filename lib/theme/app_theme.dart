import 'package:flutter/material.dart';

class LightColors {
  // ─────────────────────────────────────────────
  // BACKGROUNDS & SURFACES
  // ─────────────────────────────────────────────
  static const Color background = Color(0xFFFAFBFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFF5F7FB);
  static const Color surfaceSoft = Color(0xFFEEF1F6);

  // Cards
  static const Color card = Color(0xFFFFFFFF);
  static const Color cardAlt = Color(0xFFF8FAFD);
  static const Color challengeCard = Color(0xFF6677FF);

  // ─────────────────────────────────────────────
  // BRAND / PRIMARY
  // ─────────────────────────────────────────────
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryDark = Color(0xFF1D4ED8);
  static const Color primaryLight = Color(0xFF60A5FA);
  static const Color primarySoft = Color(0xFFDBEAFE);

  // ─────────────────────────────────────────────
  // PLAN / CATEGORY COLORS
  // ─────────────────────────────────────────────
  static const Color planCardYellow = Color(0xFFD4D913);
  static const Color planCardBlue = Color(0xFF4BA3D0);
  static const Color planCardPink = Color(0xFFE54B8C);

  static const Color accentCyan = Color(0xFF0BA8C8);
  static const Color accentViolet = Color(0xFF6B5BA8);
  static const Color accentLime = Color(0xFFD4D913);
  static const Color accentPink = Color(0xFFE54B8C);

  // ─────────────────────────────────────────────
  // TEXT
  // ─────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF0B0E15);
  static const Color textSecondary = Color(0xFF5A6580);
  static const Color textLight = Color(0xFF1A1A1A);
  static const Color textMuted = Color(0xFF8D96A8);
  static const Color textDisabled = Color(0xFFB5BDCA);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ─────────────────────────────────────────────
  // NAVIGATION
  // ─────────────────────────────────────────────
  static const Color navBackground = Color(0xFFF8F9FB);
  static const Color navActive = Color(0xFF0B0E15);
  static const Color navInactive = Color(0xFF8D96A8);

  // ─────────────────────────────────────────────
  // PROGRESS / STREAK / DAYS
  // ─────────────────────────────────────────────
  static const Color daySelected = Color(0xFF0B0E15);
  static const Color dayUnselected = Color(0xFFF5F7FB);
  static const Color dayDot = Color(0xFFD4D913);

  static const Color progress = Color(0xFF2563EB);
  static const Color progressBackground = Color(0xFFE5EAF2);

  // ─────────────────────────────────────────────
  // TAGS / CHIPS
  // ─────────────────────────────────────────────
  static const Color tagBg = Color(0x1A0B0E15);
  static const Color tagText = Color(0xFF30394A);
  static const Color chipBackground = Color(0xFFF1F4F9);
  static const Color chipBorder = Color(0xFFDCE2EC);

  // ─────────────────────────────────────────────
  // BORDERS / DIVIDERS
  // ─────────────────────────────────────────────
  static const Color border = Color(0xFFE2E7EF);
  static const Color divider = Color(0xFFEDF0F5);

  // ─────────────────────────────────────────────
  // STATUS
  // ─────────────────────────────────────────────
  static const Color success = Color(0xFF22A06B);
  static const Color successSoft = Color(0xFFE6F7EF);

  static const Color error = Color(0xFFE5484D);
  static const Color errorSoft = Color(0xFFFDEBEC);

  static const Color warning = Color(0xFFE5A900);
  static const Color warningSoft = Color(0xFFFFF5D6);

  static const Color info = Color(0xFF2563EB);
  static const Color infoSoft = Color(0xFFE8F0FF);

  // ─────────────────────────────────────────────
  // QUIZ
  // ─────────────────────────────────────────────
  static const Color quizCorrect = Color(0xFF22A06B);
  static const Color quizCorrectBg = Color(0xFFE6F7EF);

  static const Color quizWrong = Color(0xFFE5484D);
  static const Color quizWrongBg = Color(0xFFFDEBEC);

  static const Color quizSelected = Color(0xFF2563EB);
  static const Color quizSelectedBg = Color(0xFFE8F0FF);

  // ─────────────────────────────────────────────
  // GAMIFICATION
  // ─────────────────────────────────────────────
  static const Color streak = Color(0xFFFF8A00);
  static const Color streakSoft = Color(0xFFFFF0DD);

  static const Color badge = Color(0xFFEAB308);
  static const Color badgeSoft = Color(0xFFFFF7D6);

  static const Color xp = Color(0xFF8B5CF6);
  static const Color xpSoft = Color(0xFFF0EAFE);

  // ─────────────────────────────────────────────
  // OVERLAY
  // ─────────────────────────────────────────────
  static const Color overlay = Color(0x66000000);
  static const Color shadow = Color(0x140B0E15);
}

class DarkColors {
  // ─────────────────────────────────────────────
  // BACKGROUNDS & SURFACES
  // ─────────────────────────────────────────────
  static const Color background = Color(0xFF080D16);
  static const Color surface = Color(0xFF0D1420);
  static const Color surfaceAlt = Color(0xFF111A28);
  static const Color surfaceSoft = Color(0xFF172131);

  // Cards
  static const Color card = Color(0xFF101927);
  static const Color cardAlt = Color(0xFF141F2E);
  static const Color challengeCard = Color(0xFF4F5FE5);

  // ─────────────────────────────────────────────
  // BRAND / PRIMARY
  // ─────────────────────────────────────────────
  static const Color primary = Color(0xFF3B82F6);
  static const Color primaryDark = Color(0xFF2563EB);
  static const Color primaryLight = Color(0xFF60A5FA);
  static const Color primarySoft = Color(0xFF132A50);

  // ─────────────────────────────────────────────
  // PLAN / CATEGORY COLORS
  // ─────────────────────────────────────────────
  static const Color planCardYellow = Color(0xFFD4D913);
  static const Color planCardBlue = Color(0xFF3E91C2);
  static const Color planCardPink = Color(0xFFD83E7E);

  static const Color accentCyan = Color(0xFF18B9D8);
  static const Color accentViolet = Color(0xFF8B7BC7);
  static const Color accentLime = Color(0xFFD4D913);
  static const Color accentPink = Color(0xFFE54B8C);

  // ─────────────────────────────────────────────
  // TEXT
  // ─────────────────────────────────────────────
  static const Color textPrimary = Color(0xFFF5F7FB);
  static const Color textSecondary = Color(0xFFAAB5C7);
  static const Color textLight = Color(0xFFE6EBF3);
  static const Color textMuted = Color(0xFF7F8BA0);
  static const Color textDisabled = Color(0xFF4F5B6D);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ─────────────────────────────────────────────
  // NAVIGATION
  // ─────────────────────────────────────────────
  static const Color navBackground = Color(0xFF0A111C);
  static const Color navActive = Color(0xFFFFFFFF);
  static const Color navInactive = Color(0xFF718096);

  // ─────────────────────────────────────────────
  // PROGRESS / STREAK / DAYS
  // ─────────────────────────────────────────────
  static const Color daySelected = Color(0xFFFFFFFF);
  static const Color dayUnselected = Color(0xFF141E2C);
  static const Color dayDot = Color(0xFFD4D913);

  static const Color progress = Color(0xFF3B82F6);
  static const Color progressBackground = Color(0xFF263346);

  // ─────────────────────────────────────────────
  // TAGS / CHIPS
  // ─────────────────────────────────────────────
  static const Color tagBg = Color(0x1AFFFFFF);
  static const Color tagText = Color(0xFFD9E1ED);
  static const Color chipBackground = Color(0xFF141E2C);
  static const Color chipBorder = Color(0xFF263346);

  // ─────────────────────────────────────────────
  // BORDERS / DIVIDERS
  // ─────────────────────────────────────────────
  static const Color border = Color(0xFF263346);
  static const Color divider = Color(0xFF1B2635);

  // ─────────────────────────────────────────────
  // STATUS
  // ─────────────────────────────────────────────
  static const Color success = Color(0xFF32C98A);
  static const Color successSoft = Color(0xFF102D25);

  static const Color error = Color(0xFFFF6267);
  static const Color errorSoft = Color(0xFF351A1E);

  static const Color warning = Color(0xFFFFC043);
  static const Color warningSoft = Color(0xFF332A12);

  static const Color info = Color(0xFF60A5FA);
  static const Color infoSoft = Color(0xFF142B4D);

  // ─────────────────────────────────────────────
  // QUIZ
  // ─────────────────────────────────────────────
  static const Color quizCorrect = Color(0xFF32C98A);
  static const Color quizCorrectBg = Color(0xFF102D25);

  static const Color quizWrong = Color(0xFFFF6267);
  static const Color quizWrongBg = Color(0xFF351A1E);

  static const Color quizSelected = Color(0xFF3B82F6);
  static const Color quizSelectedBg = Color(0xFF142B4D);

  // ─────────────────────────────────────────────
  // GAMIFICATION
  // ─────────────────────────────────────────────
  static const Color streak = Color(0xFFFF9F1A);
  static const Color streakSoft = Color(0xFF332313);

  static const Color badge = Color(0xFFF2C94C);
  static const Color badgeSoft = Color(0xFF332D12);

  static const Color xp = Color(0xFFA78BFA);
  static const Color xpSoft = Color(0xFF241C3D);

  // ─────────────────────────────────────────────
  // OVERLAY
  // ─────────────────────────────────────────────
  static const Color overlay = Color(0x99000000);
  static const Color shadow = Color(0x66000000);
}

class AppColors {
  static const Color background = LightColors.background;
  static const Color surface = LightColors.surface;
  static const Color surfaceAlt = LightColors.surfaceAlt;
  static const Color surfaceSoft = LightColors.surfaceSoft;
  static const Color card = LightColors.card;
  static const Color cardAlt = LightColors.cardAlt;
  static const Color challengeCard = LightColors.challengeCard;
  static const Color primary = LightColors.primary;
  static const Color primaryDark = LightColors.primaryDark;
  static const Color primaryLight = LightColors.primaryLight;
  static const Color primarySoft = LightColors.primarySoft;
  static const Color planCardYellow = LightColors.planCardYellow;
  static const Color planCardBlue = LightColors.planCardBlue;
  static const Color planCardPink = LightColors.planCardPink;
  static const Color accentCyan = LightColors.accentCyan;
  static const Color accentViolet = LightColors.accentViolet;
  static const Color accentLime = LightColors.accentLime;
  static const Color accentPink = LightColors.accentPink;
  static const Color textPrimary = LightColors.textPrimary;
  static const Color textSecondary = LightColors.textSecondary;
  static const Color textLight = LightColors.textLight;
  static const Color textMuted = LightColors.textMuted;
  static const Color textDisabled = LightColors.textDisabled;
  static const Color textOnPrimary = LightColors.textOnPrimary;
  static const Color navBackground = LightColors.navBackground;
  static const Color navActive = LightColors.navActive;
  static const Color navInactive = LightColors.navInactive;
  static const Color daySelected = LightColors.daySelected;
  static const Color dayUnselected = LightColors.dayUnselected;
  static const Color dayDot = LightColors.dayDot;
  static const Color progress = LightColors.progress;
  static const Color progressBackground = LightColors.progressBackground;
  static const Color tagBg = LightColors.tagBg;
  static const Color tagText = LightColors.tagText;
  static const Color chipBackground = LightColors.chipBackground;
  static const Color chipBorder = LightColors.chipBorder;
  static const Color border = LightColors.border;
  static const Color divider = LightColors.divider;
  static const Color success = LightColors.success;
  static const Color successSoft = LightColors.successSoft;
  static const Color error = LightColors.error;
  static const Color errorSoft = LightColors.errorSoft;
  static const Color warning = LightColors.warning;
  static const Color warningSoft = LightColors.warningSoft;
  static const Color info = LightColors.info;
  static const Color infoSoft = LightColors.infoSoft;
  static const Color quizCorrect = LightColors.quizCorrect;
  static const Color quizCorrectBg = LightColors.quizCorrectBg;
  static const Color quizWrong = LightColors.quizWrong;
  static const Color quizWrongBg = LightColors.quizWrongBg;
  static const Color quizSelected = LightColors.quizSelected;
  static const Color quizSelectedBg = LightColors.quizSelectedBg;
  static const Color streak = LightColors.streak;
  static const Color streakSoft = LightColors.streakSoft;
  static const Color badge = LightColors.badge;
  static const Color badgeSoft = LightColors.badgeSoft;
  static const Color xp = LightColors.xp;
  static const Color xpSoft = LightColors.xpSoft;
  static const Color overlay = LightColors.overlay;
  static const Color shadow = LightColors.shadow;
}

class AppThemeColors {
  static bool _isDark(BuildContext context) {
    return Theme.of(context).brightness == Brightness.dark;
  }

  static Color background(BuildContext context) =>
      _isDark(context) ? DarkColors.background : LightColors.background;
  static Color surface(BuildContext context) =>
      _isDark(context) ? DarkColors.surface : LightColors.surface;
  static Color surfaceAlt(BuildContext context) =>
      _isDark(context) ? DarkColors.surfaceAlt : LightColors.surfaceAlt;
  static Color surfaceSoft(BuildContext context) =>
      _isDark(context) ? DarkColors.surfaceSoft : LightColors.surfaceSoft;
  static Color challengeCard(BuildContext context) =>
      _isDark(context) ? DarkColors.challengeCard : LightColors.challengeCard;
  static Color primary(BuildContext context) =>
      _isDark(context) ? DarkColors.primary : LightColors.primary;
  static Color primarySoft(BuildContext context) =>
      _isDark(context) ? DarkColors.primarySoft : LightColors.primarySoft;
  static Color textPrimary(BuildContext context) =>
      _isDark(context) ? DarkColors.textPrimary : LightColors.textPrimary;
  static Color textSecondary(BuildContext context) =>
      _isDark(context) ? DarkColors.textSecondary : LightColors.textSecondary;
  static Color textLight(BuildContext context) =>
      _isDark(context) ? DarkColors.textLight : LightColors.textLight;
  static Color textMuted(BuildContext context) =>
      _isDark(context) ? DarkColors.textMuted : LightColors.textMuted;
  static Color textOnPrimary(BuildContext context) =>
      _isDark(context) ? DarkColors.textOnPrimary : LightColors.textOnPrimary;
  static Color chipBackground(BuildContext context) =>
      _isDark(context) ? DarkColors.chipBackground : LightColors.chipBackground;
  static Color chipBorder(BuildContext context) =>
      _isDark(context) ? DarkColors.chipBorder : LightColors.chipBorder;
  static Color divider(BuildContext context) =>
      _isDark(context) ? DarkColors.divider : LightColors.divider;
  static Color border(BuildContext context) =>
      _isDark(context) ? DarkColors.border : LightColors.border;
  static Color shadow(BuildContext context) =>
      _isDark(context) ? DarkColors.shadow : LightColors.shadow;
  static Color overlay(BuildContext context) =>
      _isDark(context) ? DarkColors.overlay : LightColors.overlay;
  static Color card(BuildContext context) =>
      _isDark(context) ? DarkColors.card : LightColors.card;
  static Color navBackground(BuildContext context) =>
      _isDark(context) ? DarkColors.navBackground : LightColors.navBackground;
  static Color navActive(BuildContext context) =>
      _isDark(context) ? DarkColors.navActive : LightColors.navActive;
  static Color navInactive(BuildContext context) =>
      _isDark(context) ? DarkColors.navInactive : LightColors.navInactive;
  static Color accentCyan(BuildContext context) =>
      _isDark(context) ? DarkColors.accentCyan : LightColors.accentCyan;
  static Color planCardBlue(BuildContext context) =>
      _isDark(context) ? DarkColors.planCardBlue : LightColors.planCardBlue;
}

class AppTextStyles {
  static const String fontFamily = 'SF Pro Display';

  static const TextStyle greeting = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.w700,
    color: AppColors.textPrimary,
    letterSpacing: -0.3,
  );

  static const TextStyle greetingDate = TextStyle(
    fontSize: 13,
    fontWeight: FontWeight.w400,
    color: AppColors.textSecondary,
  );

  static const TextStyle sectionTitle = TextStyle(
    fontSize: 22,
    fontWeight: FontWeight.w700,
    color: AppColors.textPrimary,
    letterSpacing: -0.5,
  );

  static const TextStyle challengeTitle = TextStyle(
    fontSize: 26,
    fontWeight: FontWeight.w800,
    color: AppColors.textPrimary,
    letterSpacing: -0.8,
    height: 1.15,
  );

  static const TextStyle challengeSubtitle = TextStyle(
    fontSize: 13,
    fontWeight: FontWeight.w400,
    color: AppColors.textPrimary,
  );

  static const TextStyle planCardTitle = TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.w800,
    color: AppColors.textPrimary,
    letterSpacing: -0.5,
    height: 1.2,
  );

  static const TextStyle planCardDetail = TextStyle(
    fontSize: 13,
    fontWeight: FontWeight.w400,
    color: AppColors.textPrimary,
    height: 1.5,
  );

  static const TextStyle tagText = TextStyle(
    fontSize: 11,
    fontWeight: FontWeight.w500,
    color: AppColors.textPrimary,
  );

  static const TextStyle trainerLabel = TextStyle(
    fontSize: 10,
    fontWeight: FontWeight.w400,
    color: AppColors.textPrimary,
  );

  static const TextStyle trainerName = TextStyle(
    fontSize: 13,
    fontWeight: FontWeight.w600,
    color: AppColors.textPrimary,
  );

  static const TextStyle dayLabel = TextStyle(
    fontSize: 11,
    fontWeight: FontWeight.w500,
    letterSpacing: 0.2,
  );

  static const TextStyle dayNumber = TextStyle(
    fontSize: 15,
    fontWeight: FontWeight.w700,
    letterSpacing: -0.2,
  );
}

class AppRadius {
  static const double card = 24.0;
  static const double cardSmall = 16.0;
  static const double pill = 100.0;
  static const double tag = 100.0;
  static const double avatar = 100.0;
  static const double navBar = 32.0;
}

class AppSpacing {
  static const double screenPadding = 20.0;
  static const double cardGap = 12.0;
  static const double sectionGap = 24.0;
}

class AppTheme {
  static ThemeData getDarkTheme() {
    return ThemeData(
      useMaterial3: true,
      brightness: Brightness.dark,
      scaffoldBackgroundColor: DarkColors.background,
      primaryColor: DarkColors.primary,
      canvasColor: DarkColors.surface,
      cardColor: DarkColors.card,
      appBarTheme: AppBarTheme(
        backgroundColor: DarkColors.surface,
        elevation: 0,
        centerTitle: true,
        titleTextStyle: AppTextStyles.sectionTitle,
      ),
      bottomNavigationBarTheme: BottomNavigationBarThemeData(
        backgroundColor: DarkColors.navBackground,
        selectedItemColor: DarkColors.navActive,
        unselectedItemColor: DarkColors.navInactive,
        type: BottomNavigationBarType.fixed,
        elevation: 0,
      ),
      colorScheme: ColorScheme.dark(
        surface: DarkColors.surface,
        primary: DarkColors.primary,
        secondary: DarkColors.accentViolet,
        tertiary: DarkColors.accentLime,
      ),
    );
  }

  static ThemeData getLightTheme() {
    return ThemeData(
      useMaterial3: true,
      brightness: Brightness.light,
      scaffoldBackgroundColor: LightColors.background,
      primaryColor: LightColors.primary,
      canvasColor: LightColors.surface,
      cardColor: LightColors.card,
      appBarTheme: AppBarTheme(
        backgroundColor: LightColors.surface,
        elevation: 0,
        centerTitle: true,
        titleTextStyle: AppTextStyles.sectionTitle,
      ),
      bottomNavigationBarTheme: BottomNavigationBarThemeData(
        backgroundColor: LightColors.navBackground,
        selectedItemColor: LightColors.navActive,
        unselectedItemColor: LightColors.navInactive,
        type: BottomNavigationBarType.fixed,
        elevation: 0,
      ),
      colorScheme: ColorScheme.light(
        surface: LightColors.surface,
        primary: LightColors.primary,
        secondary: LightColors.accentViolet,
        tertiary: LightColors.accentLime,
      ),
    );
  }
}
